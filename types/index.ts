export type TaskType = {
    name: string;
    status: string;
    createdDate: string;
    deadline: string;
};

export type TaskColors = {
    Todo: string;
    Working: string;
    Done: string;
};
